
# Example environment for Cast-Identity
# If using SQLite (default), you can leave DATABASE_URL unset or use the line below:
# DATABASE_URL=sqlite:///./cast_identity.db

# For Postgres via Docker compose:
# DATABASE_URL=postgresql+psycopg://cast:cast@db:5432/cast_identity

APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=info
